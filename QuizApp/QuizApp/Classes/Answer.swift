
import UIKit

class Answer {
    let content: String
    let isCorrect: Bool
    
    init(content: String, isCorrect: Bool) {
        self.content = content
        self.isCorrect = isCorrect
    }
}
